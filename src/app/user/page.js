import Users from "@/Components/Users";

const user = () => {
    return (
        <div>
              <h1>This is user componet of SSR</h1>

             <Users/>
        </div>
    );
};

export default user;